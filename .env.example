# Database SQLite (percorso relativo a prisma/schema.prisma, portatile per il deploy self-hosted)
DATABASE_URL="file:../data/ecomemo.db"

# NextAuth.js
# Genera un valore con: openssl rand -base64 32
NEXTAUTH_SECRET="changeme"
NEXTAUTH_URL="http://localhost:3000"
